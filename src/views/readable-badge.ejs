<!DOCTYPE html>
<html lang="pt-br">
  <% if (hasData) { %><%- include("parts/head", { pageName: gitHubData.name,
  hasEvent: true, eventName: profileData.event }) %><% } else { %><%-
  include("parts/head", { pageName: "Home", hasEvent: false }) %><% } %>

  <body class="page has-img-background has-badge">
    <main class="badge">
      <header class="badge-header">
        <div class="badge-band"></div>
        <a href="/login" class="btn-login-redirect">Entrar</a>
      </header>
      <section class="badge-body">
        <header class="readable-badge-avatar">
          <img
            src="<% if (hasData) { %><%= gitHubData.avatar_url %><% } else { %>https://via.placeholder.com/160x184/e1e1e6/202024<% } %>"
            alt="<% if (hasData) { %><%= gitHubData.name %><% } else { %>Sua foto de perfil<% } %>"
            class="img-avatar"
          />
        </header>
        <section>
          <div class="identification">
            <h1 class="user-name">
              <% if (hasData) { %><%= gitHubData.name %><% } else { %>Seu nome<%
              } %>
            </h1>
            <a
              href="<% if (hasData) { %><%= gitHubData.html_url %><% } else { %>#<% } %>"
              target="_blank"
              rel="noreferrer noopener"
              class="profile-link"
            >
              <img
                src="/images/github.svg"
                alt="Ícone do GitHub"
                class="img-github"
              />
              <span>
                <% if (hasData) { %><%= gitHubData.login %><% } else {
                %>usuário<% } %>
              </span>
            </a>
          </div>
          <p class="user-biography">
            <% if (hasData) { %><%= gitHubData.bio %><% } else { %>Descrição do
            GitHub<% } %>
          </p>
        </section>
        <footer class="badge-footer">
          <ul class="social-links-wrapper">
            <li class="social-link-container">
              <a
                href="<% if (hasData) { %>https://youtube.com/<%= profileData.links.youtube %><% } else { %>#<% } %>"
                target="_blank"
                rel="noreferrer noopener"
              >
                <img
                  src="/images/youtube.svg"
                  alt="Ícone do YouTube"
                  class="img-social"
                />
              </a>
            </li>
            <li class="social-link-container">
              <a
                href="<% if (hasData) { %>https://instagram.com/<%= profileData.links.instagram %>/<% } else { %>#<% } %>"
                target="_blank"
                rel="noreferrer noopener"
              >
                <img
                  src="/images/instagram.svg"
                  alt="Ícone do Instagram"
                  class="img-social"
                />
              </a>
            </li>
            <li class="social-link-container">
              <a
                href="<% if (hasData) { %>https://facebook.com/<%= profileData.links.facebook %><% } else { %>#<% } %>"
                target="_blank"
                rel="noreferrer noopener"
              >
                <img
                  src="/images/facebook.svg"
                  alt="Ícone do Facebook"
                  class="img-social"
                />
              </a>
            </li>
            <li class="social-link-container">
              <a
                href="<% if (hasData) { %>https://twitter.com/<%= profileData.links.twitter %><% } else { %>#<% } %>"
                target="_blank"
                rel="noreferrer noopener"
              >
                <img
                  src="/images/twitter.svg"
                  alt="Ícone do Twitter"
                  class="img-social"
                />
              </a>
            </li>
          </ul>
        </footer>
      </section>
    </main>
  </body>
</html>
